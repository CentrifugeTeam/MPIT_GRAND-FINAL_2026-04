/** Макс. строк превью в таблице на UI (полный набор строк может быть больше). */
export const ANALYTICS_TABLE_PREVIEW_MAX = 10_000;

/** Верхняя граница LIMIT из поля «макс. строк» и WS max_rows. */
export const ANALYTICS_MAX_ROWS_CAP = 50_000_000;
