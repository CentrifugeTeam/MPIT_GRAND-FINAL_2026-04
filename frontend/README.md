# Frontend (SPA)

React, Vite, TypeScript. В Docker Compose порт **5173**; переменные — `.env.example`.

**Полная документация** лежит в репозитории в [`docs/frontend/`](../docs/frontend/README.md): архитектура, модули, WebSocket (`websocket-client.md`), NL-чат и отчёты (`nl-chat-and-reports.md`), SSE уведомлений через BFF. В каталоге `frontend/` кроме этого файла длинных `*.md` нет — только код и зависимости.
